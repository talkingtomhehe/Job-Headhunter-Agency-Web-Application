scheme-color: #0631BC
footer-color: #E1F3FB
bg-color: #F2FBFF
icon: https://fontawesome.com/
# xem chi tiết hơn ở figma cho từng đối tượng

GITHUB
git clone *link

tạo branch riêng để push code, KHÔNG push vào main
git checkout -b ...(tên branch)

trước mỗi lần code, tránh conflict:
git pull origin main

mỗi ngày code xong thì push lên git
git status
git add .
git commit -m '*note'
git push origin ...(tên branch)


APP
config/config.php
    # đường dẫn nhanh tới các file
    # sử dụng thay cho đường dẫn tương đối
    # vd: <?php include(COMPONENT_PATH . '/login-modal.php); ?>

views/components
    # chứa components tái sử dụng trong các file .php của pages
    # sử dụng bằng cách inlude như trên trong các file .php khác

views/layouts
    # chứa những đối tượng dùng chung cho các trang (header, footer,...)
    # include như components

views/pages
    # các trang web
    # components, layouts được include trong các file .php ở đây


CONFIG
config/db.sql # code tạo bảng trong db
    # mọi data của các field import vào config/ (vd: categories.sql)


PUBLIC
assets/css/
    components.css  # style cho các components
    layouts.css # style cho layoutsanh
    main.css # style của pages

assets/images/  # chứa logo và home-images

assets/js/ # script của các đối tượng

CÁC FILE CÒN LẠI ĐÃ HOÀN CHỈNH, KHÔNG CẦN CHỈNH SỬA:
index.php
.htacces
note.txt  # các thông tin cho mn dễ làm việc
README.md